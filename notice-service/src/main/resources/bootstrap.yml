spring:
  cloud:
    config:
      uri: https://sosohappy.net/config-service
      name: notice-config
  config:
    import: optional:configserver:https://sosohappy.net/config-service

management:
  endpoints:
    web:
      exposure:
        include: "*"

logging:
  level:
    root : INFO
    org:
      apache:
        kafka: WARN

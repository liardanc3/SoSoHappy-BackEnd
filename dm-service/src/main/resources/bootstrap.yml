spring:
  cloud:
    config:
      uri: http://127.0.0.1:8888
      name: dm-config
  config:
    import: optional:configserver:http://127.0.0.1:8888

management:
  endpoints:
    web:
      exposure:
        include: "*"

logging:
  level:
    root : INFO
    org:
      springframework:
        data:
          mongodb: DEBUG

